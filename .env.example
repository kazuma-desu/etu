# etcd connection configuration

# Comma-separated list of etcd endpoints
ETCD_ENDPOINTS=http://localhost:2379

# Alternatively, use ETCD_HOST for single endpoint (backwards compatible)
# ETCD_HOST=http://localhost:2379

# Authentication (optional)
ETCD_USERNAME=root
ETCD_PASSWORD=password

# Or use combined format (backwards compatible)
# ETCD_USERPASS=root:password
